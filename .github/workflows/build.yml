name: Build & Upload APK

on:
  # push:
  #     branches: [ "main" ]
  schedule:
    - cron: '*/10 * * * *'  # Run every 5 minutes

# on:
#   push:
#     paths:
#       - 'android/app/src/main/AndroidManifest.xml'
#       - 'lib/my_home_page.dart'
#       - 'lib/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: pip install -r requirements.txt
        
      - name: Run script to check status
        run: python 02_check_status.py
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}

      - name: Set APK File Name
        id: set_apk_name
        run: |
          # commit_message=$(git log -1 --pretty=%B)
          # apk_name="${commit_message// /_}_app.apk"
          apk_name="my_app.apk"
          echo "APK_NAME=$apk_name" >> $GITHUB_ENV
          echo $apk_name

      - name: Setup Java
        uses: actions/setup-java@v2
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.16.8'

      - name: Install dependencies
        run: flutter pub get

      - name: Build APK
        run: flutter build apk

      - name: Upload APK and App Bundle
        uses: actions/upload-artifact@v2
        with:
          name: app-artifacts
          path: build/app/outputs/flutter-apk/

      - name: FTP Transfer
        uses: joutvhu/ftp-transfer@v1
        with:
          host: ${{ secrets.FTP_SERVER }}
          port: 21
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          commands: |
            put ./build/app/outputs/flutter-apk/ ./
            delete app-release.apk.sha1
            
      - name: Run script to update status & rename file
        run: python 03_update_status_rename.py
        env:
          SMTP_HOST: ${{ secrets.SMTP_HOST }}
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USERNAME: ${{ secrets.DB_USERNAME }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
